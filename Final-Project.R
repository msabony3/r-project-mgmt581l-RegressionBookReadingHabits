movie_metadata <- read.csv("movies_metadata.csv") # Posters, backdrop, budget, revenue, release dates, languages, country, production company
credits <- read.csv("credits.csv") # Cast and crew
keywords <- read.csv("keywords.csv") # Plot keywords
ratings_small <- read.csv("ratings_small.csv") # Ratings

# Question: 